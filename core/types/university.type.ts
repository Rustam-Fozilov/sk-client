export type University = {
    id: number,
    name: string,
    address: string,
    image_link: string,
    info: string,
    website?: string,
    is_saved?: boolean,
    is_liked?: boolean,
    like_count: number,
}
